---
phase: 04-bot-activation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/kalshi/entertainment-bot.py
  - config/bots-config.json
autonomous: true
requirements:
  - EXEC-01

must_haves:
  truths:
    - "Entertainment bot is enabled in bots-config.json and starts via supervisor"
    - "Confidence threshold is lowered from 0.85 to 0.70 so realistic data-to-threshold ratios pass the filter"
    - "Entertainment bot skip rate drops below 80% when album/entertainment markets exist and HDD data is available"
  artifacts:
    - path: "config/bots-config.json"
      provides: "Entertainment bot enabled with tuned thresholds"
      contains: "\"enabled\": true"
    - path: "src/kalshi/entertainment-bot.py"
      provides: "Complete filter cascade with decision log at every skip point"
  key_links:
    - from: "config/bots-config.json"
      to: "src/kalshi/entertainment-bot.py"
      via: "config['entertainment']['confidenceThreshold'] read at startup"
      pattern: "confidenceThreshold"
---

<objective>
Debug and activate the entertainment bot by enabling it in config and tuning the filter cascade to reduce the 99.7% skip rate to below 80%.

Purpose: The entertainment bot has only ever placed 2 trades due to an overly aggressive confidence threshold (0.85) combined with being disabled in config. Lowering the threshold to 0.70 and enabling the bot allows it to trade when album data shows observed/threshold ratios of 1.05-1.10 (realistic for HDD data).

Output: Entertainment bot enabled in config with tuned confidence threshold, ready to trade album/entertainment markets when data is available.
</objective>

<execution_context>
@/Users/andeslee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andeslee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-bot-activation/04-RESEARCH.md

@config/bots-config.json
@src/kalshi/entertainment-bot.py

<interfaces>
<!-- From probability.py -- entertainment bot uses these: -->
info_arb_probability(observed, threshold, data_sigma_pct) -> float  # CDF probability
album_data_sigma(source) -> float  # Returns 0.05-0.15 sigma depending on source
quarter_kelly(edge, price_cents, max_cost_cents, bankroll_cents, fee_cents) -> (contracts, risk)

<!-- From kalshi_auth.py -- already used by entertainment bot: -->
TradeManager.log_decision(ticker, side, action, reason, **kwargs)
ScanSummary.skip(reason)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable entertainment bot and tune confidence threshold in config</name>
  <files>config/bots-config.json</files>
  <action>
In `config/bots-config.json`, update the `entertainment` section:

1. Change `"enabled": false` to `"enabled": true`
2. Change `"confidenceThreshold": 0.85` to `"confidenceThreshold": 0.70`

**Why 0.70:** The `info_arb_probability(observed, threshold, sigma=0.05)` function returns:
- At observed/threshold = 1.0: probability = 0.50 (no edge)
- At observed/threshold = 1.05: probability = 0.841 (passes 0.70 but fails 0.85)
- At observed/threshold = 1.10: probability = 0.977 (passes both)

Lowering to 0.70 captures realistic 5-10% data exceedances while still requiring meaningful signal. Do NOT change any other entertainment config values (maxTradeAmount, maxDailyTrades, maxDailyLoss, tickers, exit config). Do NOT change any other bot sections.
  </action>
  <verify>
    <automated>python3 -c "import json; c=json.load(open('config/bots-config.json')); e=c['entertainment']; assert e['enabled']==True, f'not enabled'; assert e['confidenceThreshold']==0.70, f'threshold={e[\"confidenceThreshold\"]}'; assert e['maxTradeAmount']==5; assert e['maxDailyTrades']==10; print('OK: entertainment enabled, threshold=0.70')"</automated>
  </verify>
  <done>Entertainment bot enabled with confidenceThreshold=0.70 in bots-config.json. All other entertainment config values unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add decision log entries for any uninstrumented entertainment bot filter skip points</name>
  <files>src/kalshi/entertainment-bot.py</files>
  <action>
Audit the entertainment bot filter cascade for any `continue` or `ss.skip()` calls that do NOT have a corresponding `trade_manager.log_decision()` call. The research indicates entertainment-bot.py already has good decision log coverage (ss.skip + log_decision at illiquid, no_match, threshold_parse_fail, low_confidence, low_edge, allocator_denied, kelly_zero).

Verify completeness by checking every code path that skips a market:
1. Search for bare `continue` statements inside market evaluation loops that lack a preceding `ss.skip()` or `trade_manager.log_decision()` call
2. Search for `ss.skip()` calls that are NOT followed by `trade_manager.log_decision()`
3. If any gaps exist, add the missing `trade_manager.log_decision()` call with appropriate reason string and context kwargs

For `ss.skip("no_match")` specifically -- check if there is a corresponding `log_decision` call. If not, add:
```python
trade_manager.log_decision(ticker, "skip", "skipped", "no_match",
                           title=m.get("title", "")[:80])
```

Do NOT change the filter logic, thresholds (beyond what Task 1 already changed in config), probability models, or sizing functions. Only ADD decision log calls where missing.
  </action>
  <verify>
    <automated>cd /Users/andeslee/Documents/Cursor-Projects/kalshi-trading && pytest tests/test_entertainment.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Every filter skip point in entertainment-bot.py has both ss.skip() and trade_manager.log_decision() calls. No bare `continue` statements in market evaluation loops without decision logging.</done>
</task>

</tasks>

<verification>
1. `python3 -c "import json; c=json.load(open('config/bots-config.json')); print(c['entertainment']['enabled'], c['entertainment']['confidenceThreshold'])"` outputs `True 0.7`
2. `grep -c "log_decision" src/kalshi/entertainment-bot.py` shows at least as many log_decision calls as ss.skip calls
3. `pytest tests/test_entertainment.py -x -q` passes
</verification>

<success_criteria>
- Entertainment bot is enabled in bots-config.json
- Confidence threshold lowered from 0.85 to 0.70
- All filter skip points in entertainment-bot.py have decision log entries
- Existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-bot-activation/04-01-SUMMARY.md`
</output>
