---
phase: 04-bot-activation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/kalshi/beatrelease-scanner.py
  - src/kalshi/economics-bot.py
autonomous: true
requirements:
  - EXEC-02
  - EXEC-03

must_haves:
  truths:
    - "Beatrelease scanner has complete decision logging at every skip point in the LLM pipeline"
    - "Economics bot Cleveland Fed nowcast parser handles current page structure and logs parsing strategy used"
    - "Economics bot has complete decision logging at every skip point including no_threshold and stale_nowcast"
  artifacts:
    - path: "src/kalshi/beatrelease-scanner.py"
      provides: "Beatrelease scanner with log_decision at every skip point"
    - path: "src/kalshi/economics-bot.py"
      provides: "Economics bot with robust nowcast parsing and complete decision logs"
  key_links:
    - from: "src/kalshi/economics-bot.py"
      to: "https://www.clevelandfed.org/indicators-and-data/inflation-nowcasting"
      via: "fetch_cleveland_fed_nowcast() HTTP request"
      pattern: "clevelandfed.org"
    - from: "src/kalshi/beatrelease-scanner.py"
      to: "config/bots-config.json"
      via: "beatrelease config section"
      pattern: "beatrelease"
---

<objective>
Debug the beatrelease scanner LLM pipeline and economics bot Cleveland Fed scraper to ensure both can execute trades when relevant market data exists, with complete decision log instrumentation.

Purpose: Beatrelease scanner has near-zero trades (LLM pipeline undertested, multiple skip paths). Economics bot has zero trades (Cleveland Fed scraper may be broken, page structure may have changed). Both bots need their filter cascades fully instrumented so decision logs reveal the binding constraint.

Output: Both bots with complete decision logging and verified data parsing pipelines.
</objective>

<execution_context>
@/Users/andeslee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andeslee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-bot-activation/04-RESEARCH.md

@src/kalshi/beatrelease-scanner.py
@src/kalshi/economics-bot.py

<interfaces>
<!-- From kalshi_auth.py: -->
TradeManager.log_decision(ticker, side, action, reason, **kwargs)
ScanSummary.skip(reason)
ScanSummary(bot_name, logger) -- constructor

<!-- From probability.py -- economics bot uses: -->
econ_nowcast_probability(nowcast_value, nowcast_sigma, threshold, direction) -> float
cpi_nowcast_sigma(days_to_release) -> float  # Step function: 14d=0.10%, 7d=0.06%, 1d=0.03%, 0d=0.01%
quarter_kelly(edge, price_cents, max_cost_cents, bankroll_cents, fee_cents) -> (contracts, risk)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument beatrelease scanner decision logging gaps</name>
  <files>src/kalshi/beatrelease-scanner.py</files>
  <action>
Audit the beatrelease scanner for decision log gaps. The research shows these skip points exist:
- `not_kalshi_related` -- ss.skip only, may lack log_decision
- `no_trades_extracted` -- ss.skip only, may lack log_decision
- `ticker_not_found` -- has both ss.skip + log_decision
- `ticker_validation_error` -- has both
- `stale_blog_price` -- has both
- `low_calibrated_confidence` -- has both
- `edge_too_small` -- has both
- `allocator_denied` -- has both

For each `ss.skip()` call that does NOT have a corresponding `trade_manager.log_decision()`, add the missing log_decision call. Specifically check:

1. `ss.skip("not_kalshi_related")` -- if no log_decision follows, add:
```python
trade_manager.log_decision("N/A", "skip", "skipped", "not_kalshi_related",
                           post_title=post.get("title", "")[:80],
                           url=post.get("url", ""))
```

2. `ss.skip("no_trades_extracted")` -- if no log_decision follows, add:
```python
trade_manager.log_decision("N/A", "skip", "skipped", "no_trades_extracted",
                           post_title=post.get("title", "")[:80],
                           llm_response_length=len(str(llm_result)))
```

Also verify the blog URL format in config matches what the scanner expects. The config has:
- `https://www.beatrelease.com/blog/categories/kalshi-predictions`
- `https://www.beatrelease.com/blog`

Do NOT change the LLM pipeline logic, DeepSeek prompt, confidence thresholds, or calibration discount. Only ADD missing decision log calls.
  </action>
  <verify>
    <automated>cd /Users/andeslee/Documents/Cursor-Projects/kalshi-trading && python3 -c "
import ast, sys
with open('src/kalshi/beatrelease-scanner.py') as f:
    content = f.read()
skips = content.count('ss.skip(')
decisions = content.count('log_decision(')
print(f'ss.skip calls: {skips}, log_decision calls: {decisions}')
# Every ss.skip should have a log_decision (some log_decisions are for trades, not skips)
assert decisions >= skips, f'Missing log_decision calls: {skips} skips but only {decisions} decisions'
print('OK: decision log coverage adequate')
"</automated>
  </verify>
  <done>Every ss.skip() in beatrelease-scanner.py has a corresponding log_decision() call. Decision logs will reveal whether the binding constraint is not_kalshi_related, no_trades_extracted, low_calibrated_confidence, or edge_too_small.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument economics bot decision logging gaps and verify nowcast parser</name>
  <files>src/kalshi/economics-bot.py</files>
  <action>
Two sub-tasks:

**A. Decision log gaps:**
Audit the economics bot for `ss.skip()` calls without corresponding `trade_manager.log_decision()`. The research shows these skip points:
- `no_threshold` (line ~609) -- ss.skip only
- `no_nowcast` (line ~622) -- ss.skip only
- `stale_nowcast` (line ~627) -- ss.skip only

For each, add a `trade_manager.log_decision()` call after the `ss.skip()`:

1. After `ss.skip("no_threshold")`:
```python
trade_manager.log_decision(ticker, "skip", "skipped", "no_threshold",
                           market_type=market_type, title=m.get("title", "")[:80])
```

2. After `ss.skip("no_nowcast")`:
```python
trade_manager.log_decision(ticker, "skip", "skipped", "no_nowcast",
                           market_type=market_type, title=m.get("title", "")[:80])
```

3. After `ss.skip("stale_nowcast")`:
```python
trade_manager.log_decision(ticker, "skip", "skipped", "stale_nowcast",
                           market_type=market_type, cache_age_hours=round(_nowcast_cache_age_hours(), 1))
```

**B. Nowcast parser robustness:**
In `fetch_cleveland_fed_nowcast()`, ensure the function logs which parsing strategy succeeded or failed. Check that the existing three-layer parser (BS4 structured -> regex -> cache fallback) has adequate logging at each layer. If the BS4 parser finds no data, it should log a warning before falling through to regex. If regex also fails, it should log before falling through to cache.

Verify the parsing strategies handle the case where Cleveland Fed returns a valid page but with a changed HTML structure -- the function should not silently return None without logging what happened.

Do NOT change the probability models, sigma values, or Kelly sizing. Only add decision logging and parser observability.
  </action>
  <verify>
    <automated>cd /Users/andeslee/Documents/Cursor-Projects/kalshi-trading && pytest tests/test_economics.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Economics bot has log_decision calls at no_threshold, no_nowcast, and stale_nowcast skip points. Nowcast parser logs which strategy succeeded/failed at each layer. Existing tests pass.</done>
</task>

</tasks>

<verification>
1. `grep -c "log_decision" src/kalshi/beatrelease-scanner.py` shows at least as many log_decision calls as ss.skip calls
2. `grep -c "log_decision" src/kalshi/economics-bot.py` shows at least as many log_decision calls as ss.skip calls
3. `pytest tests/test_economics.py -x -q` passes
</verification>

<success_criteria>
- Beatrelease scanner has log_decision at every skip point (not_kalshi_related, no_trades_extracted included)
- Economics bot has log_decision at no_threshold, no_nowcast, stale_nowcast skip points
- Nowcast parser logs parsing strategy at each layer
- Existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-bot-activation/04-02-SUMMARY.md`
</output>
