---
phase: 01-feedback-loop
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - scripts/backtest.py
  - scripts/analyze-performance.py
  - scripts/dashboard.py
  - scripts/dashboard.html
autonomous: true
requirements: [FEED-03, FEED-04, FEED-05]

must_haves:
  truths:
    - "npm run backtest produces non-null Brier scores per bot AND per market type"
    - "A calibration curve (reliability diagram) is viewable showing predicted-vs-actual by decile"
    - "Per-bot P&L shows realized P&L, win rate, and Sharpe ratio from settled trades"
    - "P&L is available as daily, weekly, and all-time cumulative per bot"
    - "Both gross and net-of-fees P&L are shown side by side"
    - "Rolling 30-day and all-time Sharpe are shown side by side"
    - "Metrics are persisted as JSON in data/ for S3 sync"
    - "Dashboard has endpoints for backtest results and calibration curves"
  artifacts:
    - path: "scripts/backtest.py"
      provides: "Per-bot, per-market-type, per-city Brier scores and calibration curves"
      contains: "by_market_type"
    - path: "scripts/analyze-performance.py"
      provides: "Daily/weekly/cumulative P&L, rolling Sharpe, fee separation"
      contains: "rolling_sharpe"
    - path: "data/backtest-results.json"
      provides: "Persisted Brier scores and calibration curve data"
    - path: "data/performance-metrics.json"
      provides: "Persisted P&L metrics with daily granularity"
    - path: "scripts/dashboard.py"
      provides: "API endpoints for /api/backtest and /api/calibration-curve"
      contains: "/api/backtest"
    - path: "scripts/dashboard.html"
      provides: "Chart.js calibration curve visualization"
      contains: "chart.js"
  key_links:
    - from: "scripts/backtest.py"
      to: "data/backtest-results.json"
      via: "--save flag writes JSON"
      pattern: "backtest-results.json"
    - from: "scripts/analyze-performance.py"
      to: "data/performance-metrics.json"
      via: "JSON persistence of daily P&L"
      pattern: "performance-metrics.json"
    - from: "scripts/dashboard.py"
      to: "data/backtest-results.json"
      via: "GET /api/backtest reads JSON"
      pattern: "/api/backtest"
    - from: "scripts/dashboard.html"
      to: "scripts/dashboard.py"
      via: "fetch /api/calibration-curve"
      pattern: "api/calibration-curve"
---

<objective>
Add per-market-type Brier score breakdowns, calibration curve output, comprehensive P&L metrics (daily/weekly/cumulative, rolling Sharpe, fee separation), JSON persistence, and dashboard visualization.

Purpose: These metrics answer the core question "are the models accurate and are we making money?" -- the entire feedback loop hinges on being able to see Brier scores by category and P&L by time window.
Output: Enhanced backtest.py with granular Brier breakdowns, enhanced analyze-performance.py with time-windowed P&L, new JSON output files, new dashboard API endpoints, Chart.js calibration curve in dashboard.
</objective>

<execution_context>
@/Users/andeslee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andeslee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-feedback-loop/01-CONTEXT.md
@.planning/phases/01-feedback-loop/01-RESEARCH.md
@.planning/phases/01-feedback-loop/01-01-SUMMARY.md

@scripts/backtest.py
@scripts/analyze-performance.py
@scripts/dashboard.py
@scripts/dashboard.html
@src/kalshi/trade_files.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-market-type Brier breakdowns and calibration curve output to backtest.py</name>
  <files>scripts/backtest.py</files>
  <action>
1. Update `scripts/backtest.py` to import from `trade_files` (canonical list) instead of its hardcoded 4-file list.

2. Add per-market-type Brier score breakdown. After computing per-bot Brier scores, group evaluated trades by market type using ticker prefix:
   - `KXHIGH*` -> "weather"
   - `KXBTC*`, `KXETH*` -> "crypto"
   - `KXCPI*`, `KXGDP*`, `KXJOBS*` -> "economics"
   - Album/entertainment tickers -> "entertainment"
   - Everything else -> "other"
   Compute `brier_score()` for each group. Report sample size (n) alongside each score.

3. Add per-city Brier breakdown for weather trades: parse city from ticker using `ticker_utils.py` `parse_weather_ticker()`, group by city, compute Brier per city.

4. Enhance calibration curve output: Update `calibration_table()` to accept `n_bins` parameter (default 10, fall back to 5 if any bin has <5 observations). Generate per-model calibration data (one curve per market type).

5. Update the `--save` flag JSON output (`data/backtest-results.json`) to include:
   - `per_bot_brier`: existing, keep as-is
   - `per_market_type_brier`: NEW dict of `{type: {brier, n}}`
   - `per_city_brier`: NEW dict of `{city: {brier, n}}`
   - `calibration_curves`: NEW dict of `{market_type: [{bin_label, n, predicted_avg, actual_avg, gap}]}`
   - `aggregate_brier`: existing, keep as-is
   - `timestamp`: ISO timestamp of when backtest ran

6. Add CLI table output for the new breakdowns (print to terminal alongside existing output). Flag Brier scores with n < 30 as "(low sample)" in the output.

7. Use `_atomic_write_json` from `kalshi_auth` for the JSON save (not raw file writes).
  </action>
  <verify>
    <automated>cd /Users/andeslee/Documents/Cursor-Projects/kalshi-trading && python3 scripts/backtest.py --help 2>&1 | head -5 && pytest tests/test_backtest.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>backtest.py produces per-bot, per-market-type, per-city Brier scores with sample sizes. Calibration curves generated per market type with adaptive binning. JSON output includes all new breakdowns. Existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add comprehensive P&L metrics and dashboard visualization</name>
  <files>scripts/analyze-performance.py, scripts/dashboard.py, scripts/dashboard.html, data/performance-metrics.json</files>
  <action>
1. Update `scripts/analyze-performance.py`:
   - Import from `trade_files` (canonical list) -- it already has 10 files but should use the shared module
   - Add daily P&L computation: group settled trades by date, compute per-day realized P&L per bot
   - Add weekly P&L: aggregate daily into ISO week buckets
   - Add cumulative P&L: running total per bot
   - Add rolling 30-day Sharpe: sliding window over daily_pnl, compute `(mean/std)*sqrt(252)` for each window
   - Show both gross P&L (revenue - cost) and net P&L (revenue - cost - fees) side by side in CLI table
   - Add `--save` flag (or `--json` if already exists) that writes to `data/performance-metrics.json`:
     ```json
     {
       "per_bot": {"weather": {"realized_pnl": ..., "net_pnl": ..., "win_rate": ..., "sharpe_all_time": ..., "sharpe_rolling_30d": ..., "trade_count": ..., "daily_pnl": {"2026-02-20": ...}}},
       "aggregate": {"realized_pnl": ..., "net_pnl": ..., "win_rate": ..., "sharpe_all_time": ..., "sharpe_rolling_30d": ...},
       "timestamp": "..."
     }
     ```
   - Use `_atomic_write_json` for the save

2. Update `scripts/dashboard.py`:
   - Add `GET /api/backtest` endpoint: reads `data/backtest-results.json`, returns full JSON (Brier scores + calibration curves)
   - Add `GET /api/calibration-curve` endpoint: reads `data/backtest-results.json`, returns just the `calibration_curves` section
   - Add `GET /api/performance` endpoint: reads `data/performance-metrics.json`, returns full P&L metrics
   - Follow existing endpoint patterns (check file exists, return error message if missing)

3. Update `scripts/dashboard.html`:
   - Add Chart.js via CDN: `<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>`
   - Add a "Model Quality" tab/section with:
     - Brier score table (per bot, per market type) fetched from `/api/backtest`
     - Calibration curve scatter+line chart: X=predicted probability, Y=actual frequency, with diagonal reference line (perfect calibration). One chart per market type. Use Chart.js scatter plot.
   - Add a "P&L Analytics" tab/section with:
     - Per-bot P&L table (gross, net, win rate, Sharpe) from `/api/performance`
     - Cumulative P&L line chart over time from daily_pnl data
   - Style consistently with existing dashboard sections

4. Add `data/performance-metrics.json` and `data/calibration-curves.json` (if separate from backtest-results) to `scripts/s3-sync.sh` whitelist for S3 sync.
  </action>
  <verify>
    <automated>cd /Users/andeslee/Documents/Cursor-Projects/kalshi-trading && python3 -c "import sys; sys.path.insert(0, 'src/kalshi'); exec(open('scripts/dashboard.py').read().split('if __name__')[0]); print('Dashboard imports OK')" 2>&1 | head -3 && pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>analyze-performance.py outputs daily/weekly/cumulative P&L with gross and net columns, rolling and all-time Sharpe. Dashboard serves /api/backtest, /api/calibration-curve, /api/performance. Dashboard HTML shows Brier table, calibration curve chart (Chart.js), and P&L analytics. All tests pass.</done>
</task>

</tasks>

<verification>
- `python3 scripts/backtest.py --save` produces `data/backtest-results.json` with per_market_type_brier and calibration_curves keys
- `python3 scripts/analyze-performance.py --save` (or equivalent) produces `data/performance-metrics.json` with per_bot and aggregate keys
- Dashboard starts without errors and `/api/backtest`, `/api/calibration-curve`, `/api/performance` return JSON
- Chart.js calibration curve renders in browser at localhost:3456
- `pytest tests/ -x -q` passes
</verification>

<success_criteria>
- Brier scores available at per-bot, per-market-type, per-city, and aggregate levels with sample sizes
- Calibration curves (reliability diagrams) rendered with Chart.js in dashboard for each market type
- P&L tracking shows realized gross/net, win rate, daily/weekly/cumulative, rolling 30-day and all-time Sharpe per bot
- All metrics persisted as JSON in data/ for S3 sync
- Dashboard serves three new API endpoints and two new visualization sections
</success_criteria>

<output>
After completion, create `.planning/phases/01-feedback-loop/01-02-SUMMARY.md`
</output>
