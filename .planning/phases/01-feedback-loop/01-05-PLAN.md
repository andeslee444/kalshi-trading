---
phase: 01-feedback-loop
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/backtest.py
  - scripts/reconcile-trades.py
  - scripts/audit.py
  - scripts/calibrate-sigma.py
  - tests/test_probability.py
  - tests/test_weather.py
  - tests/test_optimization.py
autonomous: true
requirements: [FEED-02, FEED-03]
gap_closure: true

must_haves:
  truths:
    - "backtest.py runs against live Kalshi API without 400 errors"
    - "All scripts using portfolio endpoints use limit=100, not limit=1000"
    - "All 530+ tests pass (including previously failing bracket probability and optimization tests)"
  artifacts:
    - path: "scripts/backtest.py"
      provides: "Corrected API pagination limit"
      contains: "limit=100"
    - path: "scripts/reconcile-trades.py"
      provides: "Corrected API pagination limit"
      contains: "limit=100"
    - path: "scripts/audit.py"
      provides: "Corrected API pagination limit"
      contains: "limit=100"
    - path: "scripts/calibrate-sigma.py"
      provides: "Corrected API pagination limit"
      contains: "limit=100"
    - path: "tests/test_weather.py"
      provides: "Calibration-isolated bracket tests"
      contains: "_reset_calibration"
    - path: "tests/test_optimization.py"
      provides: "ScanSummary and load_trades stubs for beatrelease import"
      contains: "ScanSummary"
  key_links:
    - from: "scripts/backtest.py"
      to: "Kalshi /portfolio/settlements"
      via: "limit query parameter"
      pattern: "limit=100"
    - from: "tests/test_probability.py"
      to: "src/kalshi/probability.py"
      via: "_reset_calibration() in setup/teardown"
      pattern: "_reset_calibration"
---

<objective>
Close two UAT gaps from Phase 1 verification:

1. **API pagination limit (major):** backtest.py, reconcile-trades.py, audit.py, and calibrate-sigma.py still use `limit=1000` on Kalshi portfolio endpoints, which returns HTTP 400. The fix (limit=100) was applied to analyze-performance.py in plan 01-04 but missed these four scripts.

2. **Test failures (minor):** 3 bracket probability tests fail because calibration.json changed sigma from default 2.0 to 5.5, and the test classes lack `_reset_calibration()` isolation. Additionally, test_optimization.py fails at collection because the beatrelease-scanner mock is missing `ScanSummary` and `load_trades` stubs.

Purpose: Make all Phase 1 scripts functional against live API and restore full test suite to green.
Output: 7 files patched, all tests passing.
</objective>

<execution_context>
@/Users/andeslee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andeslee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-feedback-loop/01-04-SUMMARY.md

Key pattern from 01-04: Kalshi API rejects limit=1000 on portfolio endpoints. analyze-performance.py was fixed to use limit=100. The same fix must be applied to 4 remaining scripts.

Key pattern from CLAUDE.md: "Probability tests must call `_reset_calibration()` in setup/teardown to clear cached calibration state between tests."
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix API pagination limit in 4 scripts</name>
  <files>
    scripts/backtest.py
    scripts/reconcile-trades.py
    scripts/audit.py
    scripts/calibrate-sigma.py
  </files>
  <action>
    Change `limit=1000` to `limit=100` in all Kalshi portfolio endpoint calls. Specific locations:

    **scripts/backtest.py:**
    - Line 110: `"/portfolio/settlements?limit=1000"` -> `"/portfolio/settlements?limit=100"`
    - Line 127: `"/portfolio/fills?limit=1000"` -> `"/portfolio/fills?limit=100"`

    **scripts/reconcile-trades.py:**
    - Line 43: `"/portfolio/settlements?limit=1000"` -> `"/portfolio/settlements?limit=100"`
    - Line 73: `"/portfolio/fills?limit=1000"` -> `"/portfolio/fills?limit=100"`

    **scripts/audit.py:**
    - Line 162: `"/portfolio/settlements?limit=1000"` -> `"/portfolio/settlements?limit=100"`
    - Line 179: `"/portfolio/fills?limit=1000"` -> `"/portfolio/fills?limit=100"`

    **scripts/calibrate-sigma.py:**
    - Line 65: `"/portfolio/settlements?limit=1000"` -> `"/portfolio/settlements?limit=100"`

    This is a pure string replacement -- 7 occurrences across 4 files. No logic changes. The pagination loops already handle cursor-based iteration correctly.

    Do NOT change any other limit values (e.g., market listing endpoints may use different limits).
  </action>
  <verify>
    <automated>grep -rn "limit=1000" scripts/backtest.py scripts/reconcile-trades.py scripts/audit.py scripts/calibrate-sigma.py | wc -l</automated>
    Expected: 0 (no remaining limit=1000 in these files)

    Also verify the fix is correct:
    <automated>grep -c "limit=100" scripts/backtest.py scripts/reconcile-trades.py scripts/audit.py scripts/calibrate-sigma.py</automated>
    Expected: backtest.py:2, reconcile-trades.py:2, audit.py:2, calibrate-sigma.py:1
  </verify>
  <done>All 7 instances of limit=1000 replaced with limit=100. Zero occurrences of limit=1000 remain in these 4 scripts.</done>
</task>

<task type="auto">
  <name>Task 2: Fix test isolation and mock stubs for 3 test files</name>
  <files>
    tests/test_probability.py
    tests/test_weather.py
    tests/test_optimization.py
  </files>
  <action>
    **tests/test_probability.py — TestWeatherProbability class (line ~107):**
    This class already has `_reset_calibration` imported (line 17) and uses it in other test classes (lines 385, 388, 455, 458, 500, 503). Add setup_method and teardown_method to TestWeatherProbability:

    ```python
    class TestWeatherProbability:
        def setup_method(self):
            _reset_calibration()

        def teardown_method(self):
            _reset_calibration()
    ```

    Insert these two methods at the top of the class, before the existing `test_t_forecast_well_above` method. This ensures tests use default sigma (2.0) not calibrated sigma (5.5).

    **tests/test_weather.py — TestComputeProbability class (line ~162):**
    Add the same calibration reset pattern. First, add `_reset_calibration` to the import from probability at the top of the file. The file already imports from probability -- find the existing import line and add `_reset_calibration` to it. Then add setup_method/teardown_method to TestComputeProbability:

    ```python
    class TestComputeProbability:
        def setup_method(self):
            _reset_calibration()

        def teardown_method(self):
            _reset_calibration()
    ```

    Insert these two methods at the top of the class, before the existing test methods.

    **tests/test_optimization.py — _load_beatrelease_scanner() (line ~66):**
    The fake_auth mock (line ~71-91) is missing two attributes that beatrelease-scanner.py imports at module level: `ScanSummary` and `load_trades`. Add these stubs to fake_auth before `sys.modules["kalshi_auth"] = fake_auth` (before line 92):

    ```python
    fake_auth.ScanSummary = type("ScanSummary", (), {
        "__init__": lambda self, *a, **kw: None,
        "markets_fetched": 0,
        "markets_evaluated": 0,
        "trades_placed": 0,
        "skips": {},
        "log_summary": lambda self: None,
    })
    fake_auth.load_trades = lambda *a, **kw: []
    ```

    Place these lines right after the existing `fake_auth.HealthCheckMonitor` block (after line 91) and before `sys.modules["kalshi_auth"] = fake_auth` (line 92).
  </action>
  <verify>
    <automated>cd /Users/andeslee/Documents/Cursor-Projects/kalshi-trading && python3 -m pytest tests/test_probability.py::TestWeatherProbability::test_b_forecast_at_center tests/test_weather.py::TestComputeProbability::test_bracket_forecast_at_center tests/test_weather.py::TestComputeProbability::test_bracket_moderate_distance tests/test_optimization.py --co -v 2>&1 | tail -5</automated>
    Expected: All 3 previously-failing tests pass, test_optimization.py collects without errors.

    Full suite confirmation:
    <automated>cd /Users/andeslee/Documents/Cursor-Projects/kalshi-trading && python3 -m pytest tests/ -x -q 2>&1 | tail -5</automated>
    Expected: 530+ passed, 0 failed
  </verify>
  <done>All 3 bracket probability tests pass with _reset_calibration() isolation. test_optimization.py collects and runs without ScanSummary/load_trades import errors. Full test suite is green.</done>
</task>

</tasks>

<verification>
1. `grep -rn "limit=1000" scripts/backtest.py scripts/reconcile-trades.py scripts/audit.py scripts/calibrate-sigma.py` returns no matches
2. `python3 -m pytest tests/ -q` shows 530+ passed, 0 failed
3. The 3 specific tests that were failing now pass:
   - test_probability.py::TestWeatherProbability::test_b_forecast_at_center
   - test_weather.py::TestComputeProbability::test_bracket_forecast_at_center
   - test_weather.py::TestComputeProbability::test_bracket_moderate_distance
4. test_optimization.py collects without import errors
</verification>

<success_criteria>
- Zero instances of `limit=1000` in backtest.py, reconcile-trades.py, audit.py, calibrate-sigma.py
- Full pytest suite passes (530+ tests, 0 failures, 0 collection errors)
- Both UAT gaps from 01-UAT.md are closed
</success_criteria>

<output>
After completion, create `.planning/phases/01-feedback-loop/01-05-SUMMARY.md`
</output>
