---
phase: 01-feedback-loop
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - scripts/calibrate-sigma.py
  - config/calibration.json
autonomous: true
requirements: [FEED-06]

must_haves:
  truths:
    - "config/calibration.json contains per-city sigma parameters with n > 0"
    - "Calibration uses Brier score as primary optimization target with P&L as tiebreaker"
    - "Cities with sparse data fall back to Bayesian shrinkage toward global sigma"
    - "Running npm run calibrate produces a non-empty calibration.json"
  artifacts:
    - path: "scripts/calibrate-sigma.py"
      provides: "Grid search calibration with multi-objective optimization and consistent trade files"
      contains: "from trade_files import"
    - path: "config/calibration.json"
      provides: "Per-city sigma parameters optimized from settlement data"
  key_links:
    - from: "scripts/calibrate-sigma.py"
      to: "src/kalshi/trade_files.py"
      via: "import TRADE_FILES"
      pattern: "from trade_files import"
    - from: "scripts/calibrate-sigma.py"
      to: "config/calibration.json"
      via: "writes optimized parameters"
      pattern: "calibration.json"
    - from: "src/kalshi/probability.py"
      to: "config/calibration.json"
      via: "lazy-loads sigma parameters"
      pattern: "calibration.json"
---

<objective>
Fix calibrate-sigma.py to use the canonical trade files, implement multi-objective optimization (Brier primary, P&L tiebreaker), and ensure it produces non-empty calibration.json when reconciled settlement data exists.

Purpose: Per-city sigma calibration is the final output of the feedback loop -- it proves the system can measure model accuracy and improve itself. Weather bot accuracy depends directly on calibrated sigma values replacing the hardcoded defaults.
Output: Updated calibrate-sigma.py with consistent trade files and multi-objective optimization. config/calibration.json populated with optimized parameters (n > 0 for cities with sufficient data).
</objective>

<execution_context>
@/Users/andeslee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andeslee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-feedback-loop/01-CONTEXT.md
@.planning/phases/01-feedback-loop/01-RESEARCH.md
@.planning/phases/01-feedback-loop/01-01-SUMMARY.md

@scripts/calibrate-sigma.py
@src/kalshi/probability.py
@src/kalshi/trade_files.py
@config/calibration.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix trade files and implement multi-objective calibration</name>
  <files>scripts/calibrate-sigma.py</files>
  <action>
1. Update `scripts/calibrate-sigma.py` to import from `trade_files` (canonical list) instead of its hardcoded 7-file list. Add the standard sys.path insert for src/kalshi/.

2. Fix variable naming confusion: the code uses `best_brier` but actually optimizes `log_loss`. Rename to `best_log_loss` for clarity. Add actual Brier score as a reported metric alongside log_loss.

3. Implement multi-objective optimization (per user decision):
   - Primary: minimize Brier score (not log_loss -- switch the optimization target to Brier)
   - Tiebreaker: maximize realized P&L when Brier scores are equal (within 0.001 tolerance)
   - To compute P&L for a sigma candidate: for each settled trade, recompute what half_kelly sizing would have been with that sigma, compute simulated profit/loss based on settlement outcome
   - Keep grid search (not Optuna -- per research recommendation, grid search runs in seconds)

4. Set minimum sample size threshold: require at least 10 settled trades per city for per-city calibration (with Bayesian shrinkage already in place via SHRINKAGE_K=15 for sparse cities). Set minimum 30 for global calibration. Log a warning for cities with < 10 trades.

5. Before overwriting `config/calibration.json`, save a backup to `config/calibration-backup.json` (simple copy). Print a diff summary showing old vs new sigma values for each city.

6. Ensure calibration.json output format matches what `probability.py` expects to lazy-load (check the `_load_calibration()` function in probability.py for the expected schema).
  </action>
  <verify>
    <automated>cd /Users/andeslee/Documents/Cursor-Projects/kalshi-trading && python3 scripts/calibrate-sigma.py --help 2>&1 | head -5 && pytest tests/test_calibration.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>calibrate-sigma.py uses canonical TRADE_FILES, optimizes on Brier score with P&L tiebreaker, backs up calibration.json before overwriting, has clear variable naming, and logs sample sizes with minimum thresholds. Existing calibration tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Validate calibration pipeline produces non-empty output</name>
  <files>scripts/calibrate-sigma.py, config/calibration.json</files>
  <action>
1. Run `python3 scripts/calibrate-sigma.py --dry-run` (or equivalent non-destructive mode) to verify:
   - It reads all trade files from the canonical TRADE_FILES list
   - It attempts to match trades against settlement data
   - It reports how many trades have settlement_result annotations
   - It reports which cities have sufficient data for per-city calibration

2. If no reconciled trades exist yet (n=0 for everything), verify the script:
   - Reports "0 settled trades found" clearly (not silently producing empty results)
   - Does NOT overwrite calibration.json with empty data
   - Exits cleanly with a message like "Run npm run reconcile first"

3. If reconciled trades DO exist (from Plan 01 execution), verify:
   - calibration.json is updated with n > 0 for at least one category
   - Backup file created at config/calibration-backup.json
   - Diff summary printed showing parameter changes

4. Run `pytest tests/test_calibration.py -x -q` to confirm existing tests pass.

5. Verify that `probability.py` can load the updated calibration.json without errors:
   ```python
   python3 -c "import sys; sys.path.insert(0, 'src/kalshi'); from probability import _reset_calibration, weather_probability; _reset_calibration(); p = weather_probability(75, 80, 'above', 1, 'NYC'); print(f'Test prob: {p}')"
   ```
  </action>
  <verify>
    <automated>cd /Users/andeslee/Documents/Cursor-Projects/kalshi-trading && pytest tests/test_calibration.py -x -q 2>&1 | tail -5 && python3 -c "import sys; sys.path.insert(0, 'src/kalshi'); from probability import _reset_calibration; _reset_calibration(); print('probability.py loads OK')"</automated>
  </verify>
  <done>Calibration pipeline runs end-to-end. If settlement data exists, calibration.json has non-zero entries. If no data yet, script exits gracefully without corrupting existing calibration. probability.py successfully loads the calibration file.</done>
</task>

</tasks>

<verification>
- `python3 scripts/calibrate-sigma.py --help` runs without import errors
- calibrate-sigma.py imports from trade_files (not hardcoded list)
- Script uses Brier score as primary optimization target (not log_loss)
- Backup created before overwriting calibration.json
- `pytest tests/test_calibration.py -x -q` passes
- `probability.py` loads calibration.json without errors
</verification>

<success_criteria>
- calibrate-sigma.py uses canonical TRADE_FILES from shared module
- Multi-objective optimization: Brier primary, P&L tiebreaker
- Minimum sample size enforced (10 per city, 30 global)
- Backup of calibration.json created before overwriting
- config/calibration.json populated with non-zero parameters when reconciled data exists
- probability.py successfully loads updated calibration
</success_criteria>

<output>
After completion, create `.planning/phases/01-feedback-loop/01-03-SUMMARY.md`
</output>
