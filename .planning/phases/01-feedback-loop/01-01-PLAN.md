---
phase: 01-feedback-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/reconcile-trades.py
  - scripts/backfill-settlements.py
  - src/kalshi/trade_files.py
autonomous: true
requirements: [FEED-01, FEED-02]

must_haves:
  truths:
    - "Running npm run reconcile annotates trade logs with settlement_result fields"
    - "Running npm run backfill fills in settlement data for trades missed by reconcile"
    - "Both scripts use the same canonical TRADE_FILES list"
    - "Re-running either script is idempotent -- already-annotated trades are skipped"
  artifacts:
    - path: "src/kalshi/trade_files.py"
      provides: "Canonical TRADE_FILES list shared across all scripts"
      exports: ["TRADE_FILES", "ALL_TRADE_PATHS"]
    - path: "scripts/reconcile-trades.py"
      provides: "Settlement reconciliation with consistent trade file list"
      contains: "from trade_files import"
    - path: "scripts/backfill-settlements.py"
      provides: "Per-ticker settlement backfill with consistent trade file list"
      contains: "from trade_files import"
  key_links:
    - from: "scripts/reconcile-trades.py"
      to: "src/kalshi/trade_files.py"
      via: "import TRADE_FILES"
      pattern: "from trade_files import"
    - from: "scripts/backfill-settlements.py"
      to: "src/kalshi/trade_files.py"
      via: "import TRADE_FILES"
      pattern: "from trade_files import"
---

<objective>
Create a canonical trade files module and fix reconciliation + backfill scripts to use it consistently, ensuring all historical trades across all bots are annotated with settlement outcomes from the Kalshi API.

Purpose: Settlement data is the foundation for every downstream metric (Brier scores, calibration, P&L). Without reconciled trades, calibration.json stays empty (all n=0) and no model validation is possible.
Output: Shared trade_files.py module, updated reconcile-trades.py, updated backfill-settlements.py. After running both scripts, trade logs in data/ contain settlement_result annotations.
</objective>

<execution_context>
@/Users/andeslee/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andeslee/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-feedback-loop/01-CONTEXT.md
@.planning/phases/01-feedback-loop/01-RESEARCH.md

@scripts/reconcile-trades.py
@scripts/backfill-settlements.py
@scripts/analyze-performance.py
@src/kalshi/kalshi_auth.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canonical TRADE_FILES module and update reconcile + backfill</name>
  <files>src/kalshi/trade_files.py, scripts/reconcile-trades.py, scripts/backfill-settlements.py</files>
  <action>
1. Create `src/kalshi/trade_files.py` with a canonical list of ALL trade log files. Use `analyze-performance.py` and `dashboard.py` as the reference (they have the most complete 10-file lists). The module should export:
   - `TRADE_FILES`: list of filenames (e.g., `["kalshi-trades.json", "kalshi-strategy-trades.json", ...]`)
   - `ALL_TRADE_PATHS`: list of full Path objects resolved relative to PROJECT_DIR/data/
   - Import `PROJECT_DIR` from `kalshi_auth` to resolve paths consistently

2. Update `scripts/reconcile-trades.py`:
   - Replace its hardcoded TRADE_FILES list with `from trade_files import ALL_TRADE_PATHS`
   - Ensure `conftest.py` path setup means `trade_files` is importable (it's in `src/kalshi/` which is on the path)
   - Keep the existing idempotent upsert pattern: `if trade.get("settlement_result") is not None: return False`
   - Keep existing settlement fetching logic unchanged

3. Update `scripts/backfill-settlements.py`:
   - Replace its hardcoded/glob trade file discovery with `from trade_files import ALL_TRADE_PATHS`
   - Keep existing rate limiting (0.5s sleep per 20 requests) -- it's safe at ~2.5 req/sec vs Kalshi's 10 req/sec limit
   - Keep existing idempotent skip logic for already-settled trades

4. Both scripts should add `sys.path` insert for `src/kalshi/` at the top (same pattern used by other scripts that import from kalshi_auth):
   ```python
   import sys, os
   sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'src', 'kalshi'))
   ```
  </action>
  <verify>
    <automated>cd /Users/andeslee/Documents/Cursor-Projects/kalshi-trading && python3 -c "import sys; sys.path.insert(0, 'src/kalshi'); from trade_files import TRADE_FILES, ALL_TRADE_PATHS; print(f'TRADE_FILES: {len(TRADE_FILES)} files'); assert len(TRADE_FILES) >= 10, f'Expected >=10 trade files, got {len(TRADE_FILES)}'"</automated>
  </verify>
  <done>trade_files.py exports TRADE_FILES with 10+ entries. reconcile-trades.py and backfill-settlements.py both import from it instead of using hardcoded lists. Existing idempotent and rate-limiting behavior preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Validate reconciliation pipeline end-to-end</name>
  <files>scripts/reconcile-trades.py, scripts/backfill-settlements.py</files>
  <action>
1. Run `npm run sync:down` to pull latest production trade logs from S3 (needed for reconciliation against real data). If sync fails due to missing AWS credentials, create a test trade log file at `data/kalshi-trades.json` with 2-3 sample entries that have no settlement_result field, so the pipeline can be verified structurally.

2. Run `python3 scripts/reconcile-trades.py --dry-run` (if dry-run flag exists) or inspect the script's output to verify it:
   - Discovers trade files from the canonical TRADE_FILES list
   - Attempts to fetch settlements from the Kalshi API
   - Reports how many trades were found vs how many already have settlement_result

3. Run `python3 scripts/backfill-settlements.py --dry-run` (if available) or `--report` to verify it:
   - Uses the same canonical TRADE_FILES list
   - Identifies unsettled trades
   - Reports planned API calls

4. If any import errors or path issues arise, fix them. The most likely issue is the sys.path setup for importing trade_files from src/kalshi/.

5. Run `pytest tests/ -x -q` to ensure no existing tests are broken by the trade_files refactor.
  </action>
  <verify>
    <automated>cd /Users/andeslee/Documents/Cursor-Projects/kalshi-trading && pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>reconcile-trades.py and backfill-settlements.py both run without import errors, use the canonical TRADE_FILES, and all existing tests pass. The pipeline is ready to annotate trade logs with settlement data.</done>
</task>

</tasks>

<verification>
- `python3 -c "from trade_files import TRADE_FILES; print(len(TRADE_FILES))"` returns >= 10
- `python3 scripts/reconcile-trades.py --help` runs without import errors
- `python3 scripts/backfill-settlements.py --help` runs without import errors
- `pytest tests/ -x -q` passes all existing tests
</verification>

<success_criteria>
- Canonical TRADE_FILES module exists at src/kalshi/trade_files.py with 10+ trade log filenames
- reconcile-trades.py imports from trade_files instead of hardcoded list
- backfill-settlements.py imports from trade_files instead of hardcoded/glob discovery
- Both scripts are idempotent (safe to re-run)
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-feedback-loop/01-01-SUMMARY.md`
</output>
