---
phase: quick-tasks
task: 10
total_tasks: 10
status: in_progress
last_updated: "2026-03-05T06:20:51.681Z"
---

<current_state>
Quick tasks 7 and 9 are merged to main. Quick task 10 (OrderbookSignals + CrossPlatformArb) is completed in worktree branch `worktree-agent-aef6a774` but NOT merged to main due to merge conflicts. The merge was aborted to keep main clean.
</current_state>

<completed_work>

- Quick 7: Bayesian edge model, dual-tail longshots, copula sizing, confidence-scaled Kelly — MERGED (commits 86c09e0..7b59f1a)
  - New module: `src/kalshi/strategy_engine.py` (463 lines)
  - BayesianEdgeEstimator, CorrelationAwareSizer, bayesian_kelly_multiplier, longshot_edge_sell/buy
  - 44 tests in `tests/test_strategy_engine.py`

- Quick 9: Intraday wave scheduling, settlement source checker, fill probability model — MERGED (commits 6025d2d..d51a6dd)
  - Added ScheduledScanner, SettlementSourceChecker, FillProbabilityEstimator to strategy_engine.py
  - Wave-based daemon loop in strategy-trader.py, Strategy 0 info-arb, fill probability limit adjustments
  - 30 new tests (72 total in test_strategy_engine.py)

- Quick 10: OrderbookSignals + CrossPlatformArb — COMPLETED IN WORKTREE, NOT MERGED
  - Branch: `worktree-agent-aef6a774`
  - Commits: 498b1be, 772fd1f, 727c9b5
  - OrderbookSignals class (imbalance ratio, spread dynamics, volume acceleration)
  - CrossPlatformArb class (fuzzy matching, locked profit, daily cap)
  - ArbOpportunity namedtuple, helper functions (normalize_event_text, fuzzy_match_score, etc.)
  - 24 new tests (68 total in worktree's test_strategy_engine.py)
  - Strategy 3 arb scan added to strategy-trader.py
  - Config keys: orderbookSignals, arbEnabled, arbDailyCapPct

</completed_work>

<remaining_work>

- Merge quick-10 worktree branch to main (resolve conflicts in 4 files)
- Update STATE.md with quick tasks 9 and 10
- Final docs commit

Conflict files (all additive — both sides add different classes/imports):
1. `src/kalshi/strategy_engine.py` — append OrderbookSignals, CrossPlatformArb, ArbOpportunity, POLYMARKET_FEE_PCT constant, helper functions (normalize_event_text, fuzzy_match_score, _extract_direction, _extract_numbers, validate_match, match_markets). Add `import re` and `from difflib import SequenceMatcher`.
2. `tests/test_strategy_engine.py` — append TestOrderbookSignals, TestArbOpportunity, TestCrossPlatformArb classes. Add OrderbookSignals, CrossPlatformArb, ArbOpportunity to imports.
3. `src/kalshi/strategy-trader.py` — add OrderbookSignals, CrossPlatformArb, ArbOpportunity to import block. Add Strategy 3 arb scan section to run_scan(). Add orderbook signal edge adjustment before Kelly sizing in find_longshot_sells/buys.
4. `config/bots-config.json` — add orderbookSignals (true), arbEnabled (false), arbDailyCapPct (0.20) to strategy section.

</remaining_work>

<decisions_made>

- Used REST snapshot polling for orderbook (not WebSocket) to fit synchronous scan architecture
- Fuzzy matching functions copied into strategy_engine.py (not imported from cross-platform-arb.py) to avoid module-level side effects
- arbEnabled defaults to false — user enables when ready
- Quick-10 ran in worktree isolation since quick-9 was modifying the same files on main
- Design doc approved: hierarchical Bayesian model with online learning, equicorrelation copula, both-tails Becker exploitation

</decisions_made>

<blockers>
- Merge conflicts between quick-9 (main) and quick-10 (worktree). All conflicts are additive — no actual code disagreements, just both branches adding different content to the same files. Resolution approach: cherry-pick worktree commits or manually merge by keeping both sides.
</blockers>

<context>
This session implemented the full "world-class strategy bot" design from the approved design doc at docs/plans/bot-improvements/04-strategy-bot.md. The design has 9 sections. Sections 1, 2, 4, 8 were implemented in quick-7. Sections 5, 6, 9 in quick-9. Sections 3, 7 in quick-10. All code is complete — the only remaining work is merging quick-10's worktree branch and updating STATE.md.

The worktree is at: /Users/andeslee/Documents/Cursor-Projects/kalshi-trading/.claude/worktrees/agent-aef6a774
The branch is: worktree-agent-aef6a774
</context>

<next_action>
Start with: `git merge worktree-agent-aef6a774` and resolve the 4 merge conflicts by keeping ALL content from both sides (imports, classes, config keys). Then run `pytest tests/test_strategy_engine.py -v` to verify all ~96 tests pass. Then update STATE.md with quick tasks 9 and 10. Then commit.
</next_action>
